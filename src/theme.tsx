import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
        main: "#232D88",
    },
  },
  shape: {
    borderRadius: 10,
  }
});

export default theme;
